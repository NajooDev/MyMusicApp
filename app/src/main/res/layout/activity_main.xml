<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Nội dung chính của Activity -->
    <!-- Nơi chứa fragment -->
    <FrameLayout
        android:id="@+id/container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/bottom_navigation"/>


    <!-- Thanh nhạc nằm trên thanh Menu -->
    <LinearLayout
        android:id="@+id/layout_mini_player"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:orientation="horizontal"
        android:background="?android:attr/windowBackground"
        android:padding="8dp"
        android:gravity="center_vertical"
        android:layout_above="@id/bottom_navigation"
        android:elevation="18dp"
        android:translationZ="4dp"
        android:weightSum="10">

        <ImageView
            android:id="@+id/miniCover"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_album_placeholder"
            android:layout_weight="0.6"/>

<!--    dùng Marquee cho title (cái mà di chuyển chữ theo chiều ngang nếu quá dài:
        singleLine="true" hoặc maxLines="1" buộc TextView chỉ hiển thị một dòng.
        ellipsize="marquee" bật chế độ marquee.
        marqueeRepeatLimit="marquee_forever" lặp vô hạn.
        scrollHorizontally="true" để chạy ngang.
        setSelected(true) là bắt buộc để marquee hoạt độn-->
        <TextView
            android:id="@+id/miniTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="9"
            android:layout_marginStart="12dp"
            android:singleLine="true"
            android:ellipsize="marquee"
            android:marqueeRepeatLimit="marquee_forever"
            android:scrollHorizontally="true"/>

        <ImageButton
            android:id="@+id/miniPlayPause"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/ic_pause"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:layout_weight="0.4"/>
    </LinearLayout>

    <!-- Thanh menu nằm đáy màn hình -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        app:menu="@menu/bottom_nav_menu"
        android:layout_alignParentBottom="true"/>
</RelativeLayout>
