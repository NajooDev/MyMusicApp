<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Khi item được chọn -->
    <item android:state_activated="true"
        android:drawable="@color/item_selected_background"/>
    <!-- Mặc định -->
    <item android:drawable="@color/item_default_background"/>
</selector>
